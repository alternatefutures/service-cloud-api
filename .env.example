# =============================================================================
# Cloud API — Environment Variables
# =============================================================================
# Copy to .env and fill in values. NEVER commit .env files.
# =============================================================================

# --- Server ---
PORT=1602
NODE_ENV=development
APP_URL=http://localhost:3000

# --- Database (PostgreSQL) ---
DATABASE_URL=postgresql://og@localhost:5432/alternatefutures_platform

# --- JWT ---
# IMPORTANT: Must be identical to service-auth JWT_SECRET.
JWT_SECRET=

# --- Auth Service ---
AUTH_SERVICE_URL=http://localhost:1601
# AUTH_INTROSPECTION_SECRET=

# --- Akash (for deployment orchestration — optional) ---
# Uses `akash` CLI directly (no MCP subprocess).
# AKASH_MNEMONIC=
# RPC_ENDPOINT=https://rpc.akashnet.net:443

# --- Phala Cloud (for CVM deployment orchestration — optional) ---
# PHALA_API_KEY=

# --- Subdomain Proxy (custom deployment URLs) ---
# Base domain for user deployments. URLs: {slug}-app.{domain} / {slug}-agent.{domain}
# Requires matching wildcard DNS (*.alternatefutures.ai) + Traefik Ingress.
# PROXY_BASE_DOMAIN=alternatefutures.ai
# PROXY_CACHE_TTL_MS=30000
# PROXY_CACHE_MAX_SIZE=1000

# --- Payments (Stripe — optional) ---
# STRIPE_SECRET_KEY=

# --- IPFS (optional) ---
# IPFS_GATEWAY_URL=https://ipfs.alternatefutures.ai
# PINATA_API_KEY=
# PINATA_API_SECRET=
